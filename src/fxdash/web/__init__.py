"""localhost Dashboard (SPEC_web).

The contract's first formal downstream consumer: reads outputs/ only, never
imports the attribution engine to recompute numbers; the only new math is
summation.
"""
