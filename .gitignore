__pycache__/
*.pyc
.pytest_cache/
.ipynb_checkpoints/
.env

# Everything the pipeline writes, and every cached or local input.
# Anchored at the repository root: src/fxdash/data/ is source code.
/data/
/outputs/
# static site build (python -m fxdash.web.build), published from gh-pages
/site/
